@AGENTS.md

# Workflow rule

Do not commit, push, or deploy any code change without the user's explicit
approval first. Propose the change and wait for a clear go-ahead ("yes",
"do it", "go ahead", etc.) before running `git commit`/`git push`. This
applies to every change, including small fixes — always ask first.
